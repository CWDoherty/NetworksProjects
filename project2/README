README

A simulation for a bridge network. 

The ./run script will spin up multiple instances of the bridge implementation and these instances will work together to transfer data between LAN's by implementing the spanning tree protocol.
