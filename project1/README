Project 1

A simple client that connects to a port at login.ccs.neu.edu. The server sends back math problems for the client to solve and once a sufficient amount of problems have been solved the server sends a secret flag which the client prints out and exits.
